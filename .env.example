GROQ_API_KEY=key

# Optional: model id on Groq (default is fine for the learning project)
GROQ_MODEL=llama-3.1-8b-instant

# Optional offline fallback — set true only if Ollama is installed locally
USE_OLLAMA=false
OLLAMA_MODEL=llama3.1
