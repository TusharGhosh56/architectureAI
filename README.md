# architectureAI
AI-powered codebase analyzer — upload a repo, get a real dependency graph, and chat with an agent that calls tools to explain the architecture, trace dependencies, and generate UML/use-case diagrams. Built with FastAPI, LangGraph, and RAG on free/local models.
